# Use C++20 modules with xmake

* Check the [instructions](https://xmake.io/#/guide/installation) to install `xmake`
* Command to compile the code:

  ```sh
  xmake f --toolchain=clang && xmake -v
  ```
